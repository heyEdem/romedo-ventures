# Task 5.2: Implement Contact Link Utilities

Centralize business contact configuration. Generate encoded WhatsApp URLs with the product enquiry message and `tel:+233...` links from configured numbers.

**Done when:** utility tests verify exact product context, encoding, and configured numbers.

