# Task 1.2: Define Content Models and Adapter

Define typed Product, Category, Branch, ContactConfig, and visibility models. Implement adapter methods for public published products, categories, branches, and contact data; keep CMS calls behind the adapter.

**Done when:** mixed draft/published fixtures return published records only and components consume typed adapter results.

