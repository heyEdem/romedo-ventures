# Task 2.2: Add Validation and Published Queries

Validate required fields, unique slugs, supported relationships, and visibility. Implement public queries that exclude drafts and do not expose stock quantities.

**Done when:** invalid/duplicate slugs fail and public fixtures contain no drafts.

